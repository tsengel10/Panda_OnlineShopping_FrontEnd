angular.module("panda")
        .controller("headerController", ["$rootScope", "$state", "$scope", "$http", "$mdDialog", "$mdMedia",
            function ($rootScope, $state, $scope, $http, $mdDialog, $mdMedia) {

                var vm = this;
            }
        ]);